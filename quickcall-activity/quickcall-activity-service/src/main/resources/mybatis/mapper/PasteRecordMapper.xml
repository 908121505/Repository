<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.honglu.quickcall.activity.service.dao.PasteRecordMapper" >
  
    <select id="selectOneByuserIdAndacceptId" resultType="PasteRecord" parameterType="Map">
    SELECT * from paste_record where persion_id=#{persion_id} and accept_id=#{accept_id} and is_attention=1
    </select>
  
</mapper>